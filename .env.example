
# ======================================================
# 🧠 Golem Brain (Web Gemini 大腦設定)
# ======================================================
# Google Gemini API Keys (必填)
# 說明：用於 DOM Doctor 自癒功能與與某些對話分析。
# 取得：https://aistudio.google.com/app/apikey
# 技巧：支援多組 Key 輪替 (KeyChain)，請用半形逗號 "," 分隔。

GEMINI_API_KEYS=你的Key1,你的Key2,你的Key3

# ======================================================
# ✈️ Telegram 設定 (左頭)
# ======================================================
# Telegram Bot Token (主 Golem A)
# 取得：在 Telegram 搜尋 @BotFather 申請

TELEGRAM_TOKEN=你的_Telegram_Bot_Token_貼這裡

# Telegram 驗證模式 (ADMIN | CHAT)
# ADMIN: (預設) 基於 Admin ID 驗證，僅限管理員個人使用。
# CHAT:  基於 Chat ID 驗證，僅限特定群組或頻道使用（防範單一機器人多人濫用）。

TG_AUTH_MODE=ADMIN

# Telegram Admin ID (個人管理員 ID)
# 說明：當模式為 ADMIN 時，只有此 ID 可以執行 /patch 或高風險指令。
# 取得：在 Telegram 搜尋 @userinfobot，查看 "Id" 欄位。

ADMIN_ID=你的_Telegram_User_ID_貼這裡

# Telegram Chat ID (群組/對話 ID)
# 說明：當模式為 CHAT 時，只有此 ID 對應的對話視窗能與機器人互動。
# 技巧：群組 ID 通常以負號 "-" 開頭。

TG_CHAT_ID=

# ======================================================
# 👾 Discord 設定 (右頭)
# ======================================================
# Discord Bot Token
# 取得：Discord Developer Portal -> Bot -> Reset Token
# 注意：請確保已開啟 "Message Content Intent"

DISCORD_TOKEN=你的_Discord_Bot_Token_貼這裡

# Discord Admin ID (管理員白名單)
# 說明：只有此 ID 可以執行 /patch 或高風險指令。
# 取得：Discord 設定 -> 進階 -> 開啟「開發者模式」，
#       然後對自己的頭像點右鍵 -> 複製使用者 ID。

DISCORD_ADMIN_ID=你的_Discord_User_ID_貼這裡

# ======================================================
# ⚙️ System Config (系統進階設定)
# ======================================================
# 瀏覽器資料暫存路徑
# 說明：存放 Puppeteer 的 Login Session，重啟後不用重新登入 Google。
# 預設：./golem_memory

USER_DATA_DIR=./golem_memory

# 測試模式 (通常設為 false)
# true: 只測試程式碼是否能執行，不啟動瀏覽器。
# false: 正常啟動。

GOLEM_TEST_MODE=false

# 部署模式 (SINGLE | MULTI)
# SINGLE: (預設) 單機模式，使用上述 Telegram 設定。
# MULTI:  多機模式，使用 golems.json 進行多實例配置。

GOLEM_MODE=SINGLE

# AI 大腦引擎 (gemini | deepseek)
# gemini:    (預設) 使用 Google Gemini Web 介面
# deepseek:  使用 DeepSeek Web 介面 (需登入 DeepSeek 帳號)

AI_MODEL=gemini

# Web Dashboard 埠號 (預設 3000)
DASHBOARD_PORT=3000

# ======================================================
# 🧠 Memory Engine (記憶引擎設定) 
# ======================================================
# 選擇模式：
# browser - (預設) 使用內建 memory.html，無須安裝額外軟體。
# qmd     - (進階) 使用 qmd 混合搜尋，需安裝 Bun 與 qmd。
# (進階選用) 指定 qmd 執行檔路徑，若已加入系統 PATH 則留空即可
# GOLEM_QMD_PATH=/path/to/your/qmd

GOLEM_MEMORY_MODE=browser

# ======================================================
# ☁️ OTA Update Config (空中升級設定)
# ======================================================
# 指定 /update 指令抓取原始碼的 GitHub 儲存庫
# 預設若留空則使用：https://raw.githubusercontent.com/Arvincreator/project-golem/main/

GITHUB_REPO=

ENABLE_WEB_DASHBOARD=true

# ======================================================
# 🦞 Moltbook Social Network (AI 社交網絡)
# ======================================================
# Moltbook API Key (核心憑證)
# 說明：讓 Agent 存取 Moltbook 社交平台 (發文/留言/按讚)。
# 取得方式：
# 1. 啟動 Golem 後，對他說「去註冊 Moltbook」。
# 2. 他會回傳一組 API Key，請填入下方。
# 注意：此 Key 等同於 Agent 的身分證，絕對保密。

MOLTBOOK_API_KEY=

# Agent Name (選填備註)
# 說明：你的 Agent 在 Moltbook 上的名稱 (例如: Golem_v9(golem))
# 這行只是讓你方便記憶，程式主要讀取上面的 API_KEY。

MOLTBOOK_AGENT_NAME=

PUPPETEER_REMOTE_DEBUGGING_PORT=9222
